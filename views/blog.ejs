<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog.title %></title>
    <link href="/css/tailwind.css" rel="stylesheet">
</head>
<body class="bg-gray-100 dark:bg-gray-800 min-h-screen transition-colors duration-200">
    <nav class="bg-white dark:bg-gray-900 shadow-lg p-4">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold text-blue-600 dark:text-blue-400">Blog Post</h1>
            <div class="flex items-center space-x-4">
                <button id="theme-toggle" class="p-2 rounded-lg bg-gray-200 dark:bg-gray-700">
                    <span id="theme-icon">🌑</span>
                </button>
                
                <a href="/allBlogs" class="btn-secondary">← Back</a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto p-4 max-w-3xl">
        <div class="bg-white dark:bg-gray-900 p-6 rounded-lg shadow-md">
            <h1 class="text-3xl font-bold mb-4 dark:text-gray-100"><%= blog.title %></h1>
            <p class="text-gray-600 dark:text-gray-400 text-sm mb-6"><%= blog.date %></p>
            <p class="text-gray-700 dark:text-gray-300 leading-relaxed whitespace-pre-wrap"><%= blog.content %></p>
        </div>
    </div>

    <script src="/js/theme.js"></script>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Blog</title>
    <link href="/css/tailwind.css" rel="stylesheet">
</head>
<body class="bg-gray-100 dark:bg-gray-800 min-h-screen transition-colors duration-200">
    <nav class="bg-white dark:bg-gray-900 shadow-lg p-4">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold text-blue-600 dark:text-blue-400">Edit Post</h1>
            <div class="flex items-center space-x-4">
                <button id="theme-toggle" class="p-2 rounded-lg bg-gray-200 dark:bg-gray-700">
                    <span id="theme-icon">🌑</span>
                </button>
                
                <a href="/allBlogs" class="btn-secondary">Cancel</a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto p-4 max-w-2xl">
        <div class="bg-white dark:bg-gray-900 p-6 rounded-lg shadow-md">
            <form action="/update/<%= blog.id %>" method="POST">
                <div class="mb-4">
                    <label class="block text-gray-700 dark:text-gray-300 text-sm font-medium mb-2">Title</label>
                    <input type="text" name="title" id="title" value="<%= blog.title %>" 
                        class="w-full p-2 border rounded focus:ring-2 focus:ring-blue-500 outline-none dark:bg-gray-800 dark:text-gray-100 dark:border-gray-700">
                </div>
                <div class="mb-6">
                    <label class="block text-gray-700 dark:text-gray-300 text-sm font-medium mb-2">Content</label>
                    <textarea name="content" id="content" rows="8"
                        class="w-full p-2 border rounded focus:ring-2 focus:ring-blue-500 outline-none dark:bg-gray-800 dark:text-gray-100 dark:border-gray-700"><%= blog.content %></textarea>
                </div>
                <div class="flex space-x-3">
                    <button type="submit" class="btn-primary">
                        Update Post
                    </button>
                </div>
            </form>
        </div>
    </div>

    <script src="/js/theme.js"></script>
</body>
</html>